
# Akhil's addendum to this skill - Gitea tracker integration

I may also use Gitea for issue tracking. If remote reports `localhost:3000` (https) or `localhost:2222` (ssh), that points to Gitea.

- Gitea: issues live in the repo's Gitea Issues (uses the tea CLI)
- [issue-tracker-gitea.md](./issue-tracker-gitea.md): Gitea issue tracker
